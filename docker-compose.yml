version: '3.8'

services:
  demetrius:
    build: .
    volumes:
      - .:/app
      - demetrius_data:/root/.demetrius_data
    environment:
      - PYTHONUNBUFFERED=1
    command: /bin/bash

volumes:
  demetrius_data:
